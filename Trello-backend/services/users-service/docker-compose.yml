version: "3.8"

users-service:
  build:
    context: .
    dockerfile: Dockerfile
  container_name: users-service
  ports:
    - "8081:8080"
  environment:
    - MONGODB_URI=mongodb://mongodb:27017
  depends_on:
    - mongodb


volumes:
  mongo-data:
    driver: local
